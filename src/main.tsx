import { render } from 'react-dom';
import { App } from './app';

const root = document.getElementById('root');

render(<App />, root);

// redux - global
// local - react-query

// rtk/query

// toolkit without hooks
